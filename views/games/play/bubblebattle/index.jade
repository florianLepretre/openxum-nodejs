extends ../../../../layouts/default

block head
    title Games

block neck
    link(rel='stylesheet', href='/views/index.min.css?#{cacheBreaker}')

block body
    script(src='/javascripts/bubblebattle/Bubblebattle.js')

    script(language='javascript').
        $(document).ready(function() {
            Bubblebattle.load(function() {
                new OpenXum.RealTimeGamePage(Bubblebattle, 'bubblebattle', 0, #{color}, #{opponent_color}, '#{game_type}', '#{game_id}', #{mode}, '#{user.username}', '#{owner_id}', '#{opponent_id}', #{replay});

            });
        });